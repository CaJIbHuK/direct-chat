:host
  height 100%
  display flex
  flex-direction column
  justify-content flex-start
  align-items stretch
  overflow-y hidden
  #chat-body
    overflow-y auto
    height 100%
    width 100%
    display flex
    flex-direction row
    #chat-members
      width 15%
    #chat-form
      display flex
      flex-direction column
      width 85%
      chat-history
        margin-bottom 20px
      chat-input
        height 20%