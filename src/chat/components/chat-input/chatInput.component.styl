:host
  display flex
  flex-direction column
  align-items stretch
  min-height 200px
  textarea
    box-shadow inset 0 0 10px #000000
    margin-bottom 20px
  #chat-input-buttons
    display flex
    flex-direction row
    align-items flex-start